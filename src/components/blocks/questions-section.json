{
  "collectionName": "components_blocks_questions_sections",
  "info": {
    "displayName": "QuestionsSection",
    "icon": "apps",
    "description": ""
  },
  "options": {},
  "attributes": {
    "title": {
      "type": "string",
      "default": "Vous avez des questions, on a les réponses !",
      "required": true
    },
    "description": {
      "type": "text",
      "default": "Et oui, on lit dans votre esprit :) Les questions les plus couramment posées par nos clients, c’est par ici !",
      "required": true
    },
    "button": {
      "type": "component",
      "repeatable": false,
      "component": "components.button",
      "required": true
    },
    "questions": {
      "type": "relation",
      "relation": "oneToMany",
      "target": "api::question.question"
    }
  }
}
